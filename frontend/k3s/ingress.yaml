apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend26-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend26-service
            port:
              number: 80